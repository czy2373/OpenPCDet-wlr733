# 6. 基于当前 PPT 的最小改法

这份说明的目标不是重做整套 `组会.pptx`，而是在当前 PPT 基础上，用最小改动把 `B0/B1` 放到合适位置，并和准备投稿的主线口径保持一致。

当前 PPT 文件：

- [/root/组会.pptx](/root/组会.pptx)

## 6.1 先给当前 PPT 定位

当前 PPT 的主体其实已经比较完整：

- 第 1 到第 21 页已经把：
  - `PointPillars` 主线
  - `ImageToBEV`
  - 同步 / 标定
  - `DAIR` 主线验证
  - 问题与后续方向
  基本讲清楚了。

需要补的不是整套重写，而是：

- 把 `B0` 明确放进“主线扩展”
- 把 `B1` 单独补一页
- 把 `camera-first` 留成后续，不要和当前主结果混写

## 6.2 当前 PPT 里已经有的 `B0`

从当前内容看，`B0` 已经在第 22 页出现，主题是：

- `B0` 验证图像可以直接参与最终保留/抑制决策

因此，最省事的方案不是再新开一页重复讲 `B0`，而是：

- 保留当前第 22 页
- 适当把标题改成“`B0`: 图像主导单帧扩展”
- 在页脚或最后一句里补一条：
  - `B0` 是扩展模块，不替代前端融合主线

## 6.3 建议新增的 `B1` 页

当前第 23 页基本是空的，最适合直接改成 `B1`。

### 建议标题

`B1 验证：时序一致性主要带来 suppress 收益`

### 建议正文要点

可以只放 4 条，避免太满：

1. `B1` 的目标不是再做一遍检测，而是利用短时序一致性修正 `B0` / detector 的单帧结果。
2. 当前 `B1` 最稳定的收益不是 rescue，而是 suppress。
3. 在 `WLR` 正式主链上，`B1 temporal suppress` 已实现 `0 TP / -6 FP` 的稳定收益。
4. 这说明时序层已经不是“只有想法”，而是已经能稳定识别一类坏 keep。

### 建议放的数字

- `support_hist=0`
- `matched_yolo_conf=0`
- `max_gt_iou=0`

配一句解释：

> 当前被 suppress 掉的坏框模式很一致，说明 `B1` 已经具备可解释的时序审计能力。

## 6.4 不建议在这次 PPT 里写重的内容

为了和论文主线保持一致，这次 PPT 不建议把下面几项写太重：

- 宽放开的 `camera-first birth`
- `image-only birth` 大量规则
- `owner-swap / duplicate-shadow` 细节
- `handoff_v2` 和 schema 太细的实现边界

这些内容更适合作为：

- 组会答疑时补充
- 论文后续工作
- 附录或备份页

## 6.5 最小修改方案

如果目标是“这周就能拿来继续汇报”，建议最小改法如下：

1. 保持第 1 到第 21 页基本不动。
2. 把第 22 页改成更明确的 `B0` 扩展定位。
3. 用第 23 页新增 `B1 suppress` 页面。
4. 在最后口头补一句：
   - `camera-first` 已开始形成方向，但当前论文主线仍以前端融合 + `B0/B1` 扩展为主。

## 6.6 如果后面要做论文版 PPT

后面如果单独做论文版 PPT，可以把整套压成下面 6 页：

1. 问题与动机
2. 主方法
3. `DAIR` 主结果
4. `WLR` 落地结果
5. `B0/B1` 扩展
6. 讨论与后续

这样会比当前组会版更适合投稿汇报。
